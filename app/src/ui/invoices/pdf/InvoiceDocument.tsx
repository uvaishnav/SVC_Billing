/**
 * InvoiceDocument.tsx
 * ⚠️ DEPRECATED — this wrapper is no longer used.
 * InvoicePdf.tsx is now the single document component that wraps both
 * <Document> and <Page> and renders the full layout.
 * InvoicePreviewModal.tsx and buildInvoicePayload.ts now import InvoicePdf directly.
 * Safe to delete once confirmed no other files import InvoiceDocument.
 */
export {};
